name: Test K8s Access

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check helm
        run: |
          helm version
          kubectl version --client
          kubectl get ns
          helm list -A
