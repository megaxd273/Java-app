name: Test K8s Access

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: self-hosted

    container:
      image: alpine/helm:3.12.0
      options: --entrypoint ""

    steps:
      - name: Helm test
        run: |
          helm version
          helm list -A



